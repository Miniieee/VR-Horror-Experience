using UnityEditor;
using UnityEngine;

//[CustomEditor(typeof(DisableLights))]
public class DisableLightsEditor : MonoBehaviour
{
    /*public override void OnInspectorGUI()
    {
        base.OnInspectorGUI();

        DisableLights disableLights = (DisableLights)target;

        if (GUILayout.Button("Disable All Lights"))
        {
            disableLights.Execute();
        }
    }*/
}
